---

import MainGridLayout from '../layouts/MainGridLayout.astro'

import { getEntry } from 'astro:content'
import CollaborationModes from '@components/CollaborationModes.astro'
import Markdown from '@components/misc/Markdown.astro'
import { profileConfig } from '../config'
import I18nKey from '../i18n/i18nKey'
import { i18n } from '../i18n/translation'

const collaborationPost = await getEntry('spec', 'collaboration')

const { Content } = await collaborationPost.render()
---
<MainGridLayout title={i18n(I18nKey.collaboration)} description={`Ways to collaborate with ${profileConfig.name} — freelance work, open source, and consulting.`}>
    <div class="flex flex-col gap-8 w-full">
        <!-- Intro Section -->
        <div class="card-base px-9 py-6">
            <Markdown class="mt-2">
                <Content />
            </Markdown>
        </div>

        <!-- Collaboration Modes Section -->
        <div>
            <h2 class="mb-6 text-2xl font-bold text-center text-neutral-900 dark:text-neutral-50">
                Modes of Collaboration
            </h2>
            <CollaborationModes />
        </div>
    </div>
</MainGridLayout>